import { IMongoDBConnection } from '../interfaces/interfaces';
export declare class DbConnection {
    private _host;
    private _dbName;
    constructor(host: string, dbName: string);
    connect(): Promise<IMongoDBConnection>;
    close(db: any): Promise<void>;
}
