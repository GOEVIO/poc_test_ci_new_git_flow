import * as mongoose from 'mongoose';
import { IMongoDBConnectionManager } from '../interfaces/interfaces';
export declare abstract class MongoDBConnectionManagerDecorator implements IMongoDBConnectionManager {
    protected decorated: IMongoDBConnectionManager;
    constructor(manager: IMongoDBConnectionManager);
    connect(): Promise<mongoose.Connection>;
    close(): Promise<void>;
    checkConnections(): void;
}
