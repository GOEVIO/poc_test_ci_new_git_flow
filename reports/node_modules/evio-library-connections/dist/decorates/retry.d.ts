import * as mongoose from 'mongoose';
import { IMongoDBConnectionManager } from '../interfaces/interfaces';
import { MongoDBConnectionManagerDecorator } from './decorators';
export declare class RetryDecorator extends MongoDBConnectionManagerDecorator {
    private maxRetries;
    private retryInterval;
    constructor(manager: IMongoDBConnectionManager, maxRetries?: number, retryInterval?: number);
    connect(): Promise<mongoose.Connection>;
    private sleep;
}
