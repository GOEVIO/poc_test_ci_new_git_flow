import { IWeblateRequest } from '../../schema_interfaces/weblate.interface';

export default interface ITranslationStrategy {
  execute(
    redisKey?: string,
    weblateRequest?: IWeblateRequest
  ): Promise<Object | null>;
}
