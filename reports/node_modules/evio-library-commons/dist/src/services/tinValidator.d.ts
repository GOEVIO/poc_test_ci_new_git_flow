import { ITaxIdProResponse, IGetClassificationResponse } from '../interfaces/tinValidator.interface';
import { IError } from '../interfaces/error.interfaces';
declare class TINValidator {
    static validViesVat: (countryCode: string, tinNumber: string) => Promise<boolean>;
    static getTaxIdPro: (country: string, tin: string) => Promise<ITaxIdProResponse>;
    static getClassification: (countryCode: string, tinNumber: string) => Promise<IError | IGetClassificationResponse>;
}
export default TINValidator;
