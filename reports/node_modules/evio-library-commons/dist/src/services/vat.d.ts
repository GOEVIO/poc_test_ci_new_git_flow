import { IBillingProfile } from '../interfaces/billingProfile.interface';
export declare function getVATandContryCode(billingProfile: IBillingProfile, chargerContryCode: string, countryVAT: number, userZoneVat: number): {
    VAT: number;
    countryCode: string;
};
