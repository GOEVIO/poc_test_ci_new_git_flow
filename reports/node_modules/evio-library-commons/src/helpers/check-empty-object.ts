export const isNotEmptyObject = <T = any>(obj: T) => {
  if (typeof obj !== 'object' || obj === null || Array.isArray(obj)) {
    return false
  }
  return Object.keys(obj).length > 0
}

export const isEmptyObject = <T = any>(obj: T) => {
  return !isNotEmptyObject(obj)
}
