/**
 * typesafe undefined filter. Removes the optional/undefined from the type
 */
export function isNotUndefined<T>(value: T | undefined): value is T {
  return typeof value !== 'undefined'
}

export default isNotUndefined
