/**
 * Utility type to infer the type of the items of an Array type.
 * Defaults to the same type if it's not an Array.
 */
export type Unpacked<T> = T extends Array<infer U> ? U : T

export default Unpacked
