import { Fee } from '../schema/fee.schema';
declare class FeeRepository {
    #private;
    private constructor();
    static get instance(): FeeRepository;
    findOneByCountryCode(countryCode: string): Promise<Fee | undefined>;
    findOneByCountryCodeAndZone(countryCode: string, zone: string): Promise<Fee | undefined>;
}
export declare const FeeReadRepository: FeeRepository;
export {};
