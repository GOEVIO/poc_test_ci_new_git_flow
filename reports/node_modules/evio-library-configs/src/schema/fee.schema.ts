import { z } from 'zod'

export const FeeSchema = z.object({
  _id: z.string(),
  countryCode: z.string(),
  zone: z.string(),
  clientName: z.string(),
  fees: z.object({
    IEC: z.number(),
    IVA: z.number(),
  }),
})

export type Fee = z.infer<typeof FeeSchema>
