import { Connection, Channel } from "amqplib";
import { IAssertQueueParams, IConsumeParams, IHandleErrorParams } from './interfaces';
declare class RabbitMQConnection {
    connection: Connection;
    channel: Channel;
    private connected;
    connect(): Promise<void>;
    assertQueue(params: IAssertQueueParams): Promise<void>;
    consume(consumerParams: IConsumeParams): Promise<void>;
    handleQueueError(params: IHandleErrorParams): Promise<void>;
    sendToQueue(queue: string, message: any): Promise<void>;
    sendAlertToBackendTeam(text: string, teamsWebhookUrl: string): Promise<Response>;
}
declare const mqConnection: RabbitMQConnection;
export default mqConnection;
