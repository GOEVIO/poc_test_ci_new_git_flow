import { ISentinel } from "@/interfaces/interfaces";

const sentinelPort: number = Number(process.env.sentinelPort || 26379);

export const sentinelsConfig: ISentinel[] = [
    { host: process.env.sentinelHost1 || 'redis-sentinel1', port: sentinelPort },
    { host: process.env.sentinelHost2 || 'redis-sentinel2', port: sentinelPort },
    { host: process.env.sentinelHost3 || 'redis-sentinel3', port: sentinelPort },
];

export const masterName = process.env.masterName || 'mymaster';

export const commandTimeoutInMilli = Number(process.env.commandTimeout || 900);

export const serviceName = process.env.SERVICE_NAME || 'some-service';
