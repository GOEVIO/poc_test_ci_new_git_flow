import Redis from "ioredis";
import { Value } from "./types/types";
declare class RedisConnection {
    connection: Redis;
    private connected;
    connect(): Promise<void>;
    disconnect(): Promise<void>;
    get(key: string): Promise<string | null>;
    set(key: string, value: Value): Promise<void>;
    hset(key: string, value: Value): Promise<void>;
    setWithMilliseconds(key: string, value: Value, timeValue: number): Promise<void>;
    setWithSeconds(key: string, value: Value, timeValue: number): Promise<void>;
    delete(key: string): Promise<void>;
    keys(pattern: string): Promise<string[]>;
}
declare const redisConnection: RedisConnection;
export default redisConnection;
