apiVersion: apps/v1
kind: Deployment
metadata:
  name: assets
  namespace: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: assets
  template:
    metadata:
      labels:
        app: assets
    spec:
      containers:
        - name: assets
          image: image
          ports:
            - containerPort: 3103
          resources:
            limits:
              cpu: "150m"
              memory: "300Mi"
            requests:
              cpu: "90m"
              memory: "120Mi"
          args:
            - npm
            - run
            - start:prod
          envFrom:
            - configMapRef:
                name: assets
            - configMapRef:
                name: shared-envs
            - secretRef:
                name: shared-secrets
